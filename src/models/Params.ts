/**
 * Optional paramaters for your extension. Either defined in the JSON Schema (instance) or defined in the registry (installation)
 */
export interface Params {
  instance: any;
  installation: any;
}
